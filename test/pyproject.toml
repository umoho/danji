[project]
name = "danji-test"
version = "0.1.0"
description = "danji-cli 胆味测试工具"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.12",
    "matplotlib>=3.8",
    "soundfile>=0.12",
]

[tool.hatch.build.targets.wheel]
packages = []
